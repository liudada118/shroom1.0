// Stable route compatibility entry. The implementation lives with the
// display-system extension so the feature can evolve as one unit.
export { default } from '../../extensions/display-system/DisplaySystemBuilder.jsx';
