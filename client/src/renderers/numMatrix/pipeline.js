/**
 * pipeline.js - re-export 壳
 *
 * 实现已搬到 `@shroom/frontend/core/numMatrix/pipeline.js`，那 33 条
 * 785 点逐点比对的测试跟着一起走了（`core/numMatrix/pipeline.test.js`）。
 *
 * 它能被搬进零依赖层，正是因为它是纯的 —— 与它当初能做逐点比对是同一个性质。
 */

export * from '@shroom/frontend/core/numMatrix/pipeline.js';
