# CSV 下载实现逻辑说明

本文只描述 CSV 下载功能的实现逻辑，不绑定具体代码文件、接口名或函数名。

## 1. 下载入口逻辑

用户需要先在历史数据列表中选择一条或多条采集记录。

点击下载后，系统不会立即导出文件，而是先进入下载确认流程：

1. 检查是否已经选择历史数据。
2. 如果没有选择，提示用户先选择数据。
3. 如果已经选择，弹出下载配置窗口。
4. 在窗口中展示当前保存路径。
5. 用户可以修改保存路径。
6. 用户可以选择保存文件夹。
7. 用户可以直接打开当前保存文件夹。
8. 用户可以选择导出格式。
9. 用户可以选择需要导出的字段。
10. 用户确认后，系统开始导出。

## 2. 保存路径逻辑

系统会维护一个当前下载路径。

下载路径的来源按优先级处理：

1. 用户本次手动设置的路径。
2. 软件运行期间已经保存过的路径。
3. 系统默认下载目录。
4. 用户桌面目录。
5. 用户 Downloads 目录。
6. 用户 Documents 目录。
7. 软件内置的数据目录。

系统选择路径时，不只是保存字符串，还会检查这个路径是否真的可用。

检查逻辑：

1. 如果目录不存在，尝试自动创建。
2. 如果创建失败，提示路径不可用。
3. 如果目录存在，尝试写入一个临时测试文件。
4. 测试写入成功后，再删除测试文件。
5. 写入和删除都成功，才认为该目录可写。
6. 如果当前路径不可写，继续尝试下一个兜底路径。
7. 如果所有路径都不可写，下载失败并提示错误。

这样做的目的是避免用户选择了只读目录、无权限目录或不存在且无法创建的目录，导致导出过程失败。

## 3. 自定义路径逻辑

用户可以通过两种方式设置路径：

1. 手动输入路径。
2. 通过系统文件夹选择窗口选择路径。

用户输入或选择路径后，系统会立即做路径校验。

校验通过后：

1. 更新当前下载路径。
2. 后续导出优先使用该路径。
3. 在界面上同步显示最新路径。
4. 尝试把路径记录到本地配置中，方便后续使用。

校验失败时：

1. 不进入下载。
2. 保留当前可用路径。
3. 提示用户路径不可用或创建失败。

## 4. 导出字段逻辑

下载前，系统会根据用户选择的历史数据生成可导出字段。

字段不是固定死的，而是根据记录中的实际矩阵数据生成。

处理逻辑：

1. 读取选中记录的首帧数据。
2. 判断这条记录里有哪些有效矩阵。
3. 过滤无效矩阵。
4. 根据矩阵类型生成字段。
5. 默认选中全部字段。
6. 用户可以全选。
7. 用户可以清空。
8. 用户可以只选择部分字段。

常见导出字段包括：

1. 时间戳。
2. 设备 MAC。
3. 最大压强。
4. 最大压强坐标。
5. 平均压强。
6. 受力面积。
7. 原始矩阵数据。
8. 有效点数。
9. 总压力。
10. 备注。

如果用户取消所有字段，系统会阻止下载，并提示至少选择一个导出字段。

## 5. 下载执行逻辑

用户点击开始下载后，系统按以下顺序执行：

1. 再次确认是否有选中的历史数据。
2. 再次确认是否有选中的导出字段。
3. 校验当前下载路径是否可写。
4. 关闭下载配置窗口。
5. 展示下载进度窗口。
6. 根据用户选择的记录读取历史数据。
7. 根据用户选择的字段生成导出内容。
8. 把导出文件写入目标路径。
9. 写入成功后返回生成的文件路径。
10. 下载进度变为完成状态。
11. 界面展示已生成的文件列表。

下载进度窗口主要用于给用户明确反馈。

实际文件生成由后台完成，前端只负责展示当前状态：

1. 下载中。
2. 下载完成。
3. 下载失败。

## 6. 历史数据读取逻辑

导出时，系统会根据用户选择的采集记录读取对应的历史帧数据。

处理逻辑：

1. 找到对应采集记录。
2. 读取该记录下的所有帧。
3. 如果记录为空，跳过或返回失败。
4. 读取第一帧判断矩阵结构。
5. 后续每一帧按同样结构处理。
6. 每帧只解析一次，避免重复处理造成性能浪费。

如果一条记录中包含多个矩阵，例如坐垫和靠背，系统会把这些矩阵按同一帧时间合并到同一行导出。

这样导出的数据更适合后续用表格软件查看和分析。

## 7. 数据计算逻辑

每一帧导出前，系统会对矩阵数据做统计计算。

主要计算内容：

1. 最大压强。
2. 最大压强所在坐标。
3. 平均压强。
4. 非零最小压强。
5. 压力总和。
6. 受力面积。
7. 有效受力点数。
8. 原始矩阵数据。

坐标计算逻辑：

1. 矩阵数据本身是一维数组。
2. 系统根据矩阵宽度把一维索引转换成行列坐标。
3. 最大值所在位置会转换成坐标写入导出文件。

面积计算逻辑：

1. 先统计大于 0 的点数。
2. 如果当前矩阵有点距配置，则用点数乘以单点面积。
3. 再转换成面积单位。
4. 如果没有点距配置，则直接使用有效点数作为面积参考。

## 8. 框选数据逻辑

如果用户对历史数据保存过框选区域，导出时会优先处理框选区域。

处理逻辑：

1. 判断当前记录是否有框选信息。
2. 如果没有框选信息，导出整张矩阵。
3. 如果有框选信息，根据框选的起止行列截取矩阵子区域。
4. 只对框选区域计算最大值、平均值、面积、总压力等指标。
5. 框选区域的最大值坐标会加上框选偏移，转换成原矩阵中的真实坐标。
6. 导出文件中同时保留框选区域的原始数据。

这样用户下载的数据会和软件内看到的框选分析结果保持一致。

## 9. 压力换算逻辑

不同设备或系统类型可能有不同的压力换算规则。

导出时，系统会根据矩阵类型判断是否需要换算。

处理逻辑：

1. 普通矩阵按原始统计值导出。
2. 特定类型矩阵使用对应换算公式。
3. 最大值、平均值、框选最大值、框选平均值都会保持同一套换算规则。
4. 文件中会写入压力单位和换算说明，方便后续追溯。

这样可以避免不同设备的数据在导出后单位不一致。

## 10. 文件生成逻辑

系统会根据用户选择的格式生成文件。

CSV 生成逻辑：

1. 根据导出字段生成表头。
2. 根据每一帧计算结果生成数据行。
3. 把表头和数据行写入 CSV 文件。
4. 写入完成后检查文件编码标识。
5. 如果缺少 UTF-8 BOM，则补充 BOM。
6. 这样可以减少表格软件打开中文表头时乱码的情况。

表格文件生成逻辑：

1. 根据导出字段生成第一行表头。
2. 后续每一行写入帧数据。
3. 自动设置列宽。
4. 生成可直接打开的表格文件。

## 11. 文件命名逻辑

导出文件名由系统类型和历史记录名称组成。

基本规则：

1. 使用当前系统类型作为文件名前缀。
2. 使用历史记录名称作为文件名主体。
3. 如果历史记录设置了别名，优先把别名加入文件名。
4. 文件名中的非法字符会被替换，避免保存失败。
5. 不同记录会生成不同文件。

示例逻辑：

```text
系统类型_记录名称.csv
系统类型_别名_记录名称.csv
```

这里的示例只表达命名规则，不代表固定代码格式。

## 12. 下载结果处理逻辑

后台导出完成后，会把生成的文件路径返回给界面。

界面处理逻辑：

1. 收集所有成功生成的文件路径。
2. 从完整路径中提取文件名。
3. 如果没有拿到任何文件路径，认为下载失败。
4. 如果拿到文件路径，下载进度显示为完成。
5. 下载完成弹窗展示文件列表。
6. 每个文件旁边提供“点击打开”。
7. 弹窗底部提供“打开下载文件夹”。

如果导出多条历史记录，完成弹窗会展示多个文件。

## 13. 软件内打开文件逻辑

下载完成后，用户可以直接在软件里打开导出的 CSV。

打开文件逻辑：

1. 用户点击某个导出文件。
2. 系统先检查文件路径是否存在。
3. 如果文件存在，调用操作系统默认打开方式。
4. CSV 通常会由 Excel、WPS 或系统默认表格软件打开。
5. 如果打开文件失败，系统尝试打开文件所在文件夹。
6. 如果文件不存在，提示文件不存在或打开失败。

这不是在软件内部重新渲染 CSV，而是通过操作系统打开本地文件。

## 14. 软件内打开文件夹逻辑

用户也可以直接打开下载目录。

打开文件夹逻辑：

1. 用户点击打开文件夹。
2. 系统检查目录是否存在。
3. 如果目录存在，调用操作系统文件管理器打开。
4. Windows 下打开资源管理器。
5. macOS 下打开 Finder。
6. Linux 下打开默认文件管理器。
7. 如果目录不存在，提示路径不存在。

这样用户可以快速定位导出的文件，不需要手动复制路径去查找。

## 15. 异常处理逻辑

CSV 下载过程中主要处理这些异常：

1. 未选择历史数据。
2. 未选择导出字段。
3. 下载路径为空。
4. 下载路径无法创建。
5. 下载路径不可写。
6. 历史记录为空。
7. 数据库未准备好。
8. 文件写入失败。
9. 生成后没有拿到文件路径。
10. 打开文件失败。
11. 打开文件夹失败。

异常处理原则：

1. 下载前能发现的问题，提前阻止下载。
2. 路径问题优先尝试兜底路径。
3. 导出失败时显示失败状态。
4. 打开文件失败时尝试打开所在文件夹。
5. 不让用户误以为文件已经成功生成。

## 16. 完整流程汇总

```text
选择历史数据
  -> 点击下载
  -> 检查选择状态
  -> 打开下载配置窗口
  -> 加载可导出字段
  -> 用户确认路径、格式、字段
  -> 校验保存路径
  -> 读取历史帧数据
  -> 识别有效矩阵
  -> 计算压力、面积、坐标等指标
  -> 处理框选区域
  -> 按用户选择字段生成表头
  -> 写入 CSV 文件
  -> 补充编码标识
  -> 返回生成文件路径
  -> 展示下载完成窗口
  -> 用户点击打开文件或文件夹
  -> 调用系统默认程序打开
```

