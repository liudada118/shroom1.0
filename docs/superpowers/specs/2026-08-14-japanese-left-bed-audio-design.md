# 日文离床告警音频文案精简设计

**日期：** 2026-08-14
**目标分支：** `Revise`

## 目标

将日文离床告警 MP3 的播报内容从 `離床しました` 精简为 `離床`，其余日文告警、运行时映射和界面翻译保持不变。

## 变更范围

- 使用现有声音 `ja-JP-NanamiNeural`、语速 `-5%`，以准确文本 `離床` 重新生成 `left-bed.mp3`。
- 同步替换源码资源 `client/public/audio/alerts/ja/left-bed.mp3` 和已构建资源 `build/audio/alerts/ja/left-bed.mp3`。
- 更新 `ARCHITECTURE.md` 中离床音频的文案记录。

## 不变范围

- 不修改 `leftBed` 告警键及其音频路径。
- 不修改 `edge-seat.mp3`、`emergency.mp3`。
- 不修改日文语音回退、中文/英文播报、界面翻译或告警判断逻辑。
- 不增加运行时网络依赖或系统日文语音包依赖。

## 验证

- 两份 `left-bed.mp3` 非空且 SHA-256 完全一致。
- 音频可由本地解码器读取，时长大于零。
- 另外两份日文告警音频的文件哈希保持不变。
- 运行与日文告警播放相关的专项测试，并完成前端构建；已知无关的全套测试基线问题不作为本次阻断项。

## 提交

音频替换、架构文档更新及构建资源同步完成并验证后，以独立提交保存到 `Revise`。
