---

mindmap-plugin: basic

---

# 智能座椅气囊控制系统架构图

## 总览架构

-
  ```mermaid
  
  flowchart LR
  
  UI["React Native UI"] --> Bridge["Kotlin Native Bridge"]
  
  Bridge --> Serial["USB 串口 / CH340"]
  
  Serial --> Parser["FrameParser"]
  
  Parser --> Py["Chaquopy Python"]
  
  Py --> Algo["IntegratedSeatSystem"]
  
  Algo --> Bridge
  
  Bridge --> UI
  
  UI --> GL["Three.js / Expo GL"]
  
  ```


## 分层结构
- Sub title
	- React Native 层
		- 页面导航
		- 首页状态
		- 自定义气囊
		- 配置弹窗
		- 实时数据弹窗
		- 3D 座椅渲染
- Sub title
	- Kotlin Native 层
		- USB 权限
		- 串口连接
		- 串口读取
		- 帧解析
		- 协议组帧
		- 自动写入
		- Python 桥接
		- Native 事件
- Sub title
	- Python 算法层
		- 配置加载
		- 离座检测
		- 活体检测
		- 体型识别
		- 自适应控制
		- 偏好记录
		- 控制指令生成
- Sub title
	- 硬件层
		- 压力传感器
		- 气囊控制板
		- USB 串口
		- 24 路气囊协议

## 核心数据流

-
  ```mermaid
  
  sequenceDiagram
  
  participant HW as 硬件传感器
  
  participant AB as 气囊控制板
  
  participant KT as Kotlin 串口层
  
  participant PY as Python 算法
  
  participant RN as React Native
  
  participant GL as 3D 可视化
  
  
  
  HW->>KT: 144 字节压力帧
  
  KT->>KT: FrameParser 解析
  
  KT->>RN: onSerialData
  
  KT->>PY: server(sensorData)
  
  PY->>PY: process_frame
  
  PY->>KT: result JSON
  
  KT->>RN: onSerialResult
  
  RN->>GL: 更新压力云图
  
  PY->>KT: airbag_command
  
  KT->>AB: 55 字节控制帧
  
  AB->>KT: 55 字节气囊回传帧
  
  KT->>RN: onNonStandardFrame
  
  RN->>RN: parseAirbagCommand
  
  RN->>RN: 更新 commandStates
  
  ```


## 气囊回传展示流

-
  ```mermaid
  
  flowchart TD
  
  Board["气囊控制板"] --> Raw55["55 字节原始回传帧"]
  
  Raw55 --> Parser["FrameParser"]
  
  Parser --> Payload51["51 字节 payload"]
  
  Payload51 --> NativeEvent["onNonStandardFrame"]
  
  NativeEvent --> Frontend["React Native"]
  
  Frontend --> Parse["parseAirbagCommand"]
  
  Parse --> CommandStates["commandStates"]
  
  CommandStates --> SeatDiagram["SeatDiagram"]
  
  CommandStates --> CustomSeatDiagram["CustomSeatDiagram"]
  
  ```


## 控制链路

-
  ```mermaid
  
  flowchart TD
  
  Sensor["压力帧 144B"] --> Parser["FrameParser"]
  
  Parser --> Standard{"标准帧?"}
  
  Standard -->|"144B"| Python["Python 算法"]
  
  Standard -->|"51B"| Mode["模式帧处理"]
  
  Standard -->|"其他"| NonStd["回传面板"]
  
  
  
  Python --> Result["算法结果 JSON"]
  
  Result --> State["座椅状态"]
  
  Result --> Body["体型结果"]
  
  Result --> Command["气囊指令"]
  
  
  
  Command --> AutoWrite["Native 自动写入"]
  
  AutoWrite --> Frame55["55B 控制帧"]
  
  Frame55 --> Airbag["气囊控制板"]
  
  ```


## 模块边界
- Sub title
	- App
		- 页面切换
		- 全局状态
		- 体型缓存
		- Toast 提示
- Sub title
	- HomeScreen
		- 串口连接
		- 事件订阅
		- 算法解析
		- 3D 数据
		- 配置管理
- Sub title
	- CustomAirbagScreen
		- 手动调节
		- 气囊保存
		- 偏好记录
		- 保压指令
- Sub title
	- SerialModule
		- RN Bridge
		- USB 权限
		- Python 调用
		- 协议封装
		- 自动写入
		- 本地存储
- Sub title
	- SerialManager
		- 设备枚举
		- 串口打开
		- 字节写入
		- 连接关闭
- Sub title
	- IntegratedSeatSystem
		- 状态机
		- 检测器
		- 控制策略
		- 指令队列
		- 偏好管理

## 关键运行路径
- Sub title
	- 传感器输入
		- USB 串口读取
		- 字节流缓冲
		- 帧头识别
		- 标准帧输出
- Sub title
	- 算法处理
		- 压力矩阵归一化
		- 入座状态判断
		- 活体检测
		- 体型识别
		- 气囊控制决策
- Sub title
	- UI 展示
		- 座椅状态
		- 体型结果
		- 气囊状态
		- 压力云图
		- 实时算法数据
- Sub title
	- 气囊控制
		- 自动控制
		- 手动调节
		- 覆盖层合并
		- 保压指令
		- 停止全部气囊
- Sub title
	- 气囊回传
		- 55 字节原始帧
		- 51 字节 payload
		- 非标准帧事件
		- 指令状态解析
		- 气囊箭头展示

## 架构风险
- Sub title
	- 大模块聚合
		- HomeScreen 过重
		- SerialModule 过重
		- CarAirRN 过重
		- IntegratedSeatSystem 过重
- Sub title
	- 协议分散
		- JS 映射
		- Kotlin 映射
		- Python 配置
		- 帧格式常量
- Sub title
	- 算法包混杂
		- release_package
		- release_package(7)
		- newPy
		- 根目录算法文件
		- 历史模型产物
- Sub title
	- 构建脆弱
		- node_modules patch
		- Chaquopy 生成脚本 patch
		- 多锁文件并存
		- release 使用 debug keystore

## 新节点
- Sub title
	- 协议契约层
		- 帧长度
		- 帧头帧尾
		- 气囊 ID
		- 档位定义
		- Zone 映射
- Sub title
	- 状态中心
		- 连接状态
		- 自适应状态
		- 体型状态
		- 气囊状态
		- 算法状态
- Sub title
	- 测试节点
		- FrameParser 测试
		- 协议组帧测试
		- 算法 JSON 测试
		- Native Bridge Mock
		- 3D 渲染 Smoke
- Sub title
	- 工程治理
		- 清理历史包
		- 统一包管理器
		- 正式签名
		- 构建脚本收敛
		- 架构文档修复