# 后台「传感器类型管理」对接 / 补齐提示词

> 整段交给后台（密钥管理系统）的开发者或 AI。
> **现状问题：后台 `GET /sensorTypes` 返回的清单缺了下面这些原有的授权传感器类型，导致桌面端这些类型没显示全。**

## 需要做到两件事

1. **现在就把下面 24 个类型全部展示齐全**：后台数据库 + 管理界面 + `/sensorTypes` 接口都要**完整包含**这 24 个原有类型，一个都不能少（现在缺的全部补回）。
2. **支持以后新增**：在展示全 24 个的基础上，保留"新增传感器类型"的功能，方便以后扩展。

> 约束：原有这 24 个**只增不删、不改 `value`**（`value` 是桌面端识别键，改了就认不出；改 label 可以）。

---

## 背景

桌面端（Shroom 采集软件）启动时同步一次后台的传感器类型清单：

```
GET {BASE_URL}/sensorTypes      // 纯 REST + CORS，非 tRPC，无需 superjson
```

桌面端用它来：把**授权到的 `value` 翻译成中文名**展示（授权页 / 授权状态），以及作为传感器类型的同步数据源。

这 24 项就是桌面端授权页 `License.jsx` 里原本写死的 `SENSOR_GROUPS`（分组：常用 / 关怀 / lab / 定制 / 精密）。
后台目前少了其中一部分，所以那些类型在依赖 `/sensorTypes` 的地方不显示。请按下表把 24 项**全部补齐**。

> 重要：`value` 是桌面端识别授权类型的唯一键，**大小写敏感、必须与下表逐字一致**（注意是 `14x20高速` 这种小写 x、`32*32(检测点)` 用半角括号）。改 label 可以，改 `value` 会让桌面端识别不到。

---

## 接口契约（必须保持这个返回结构）

```jsonc
GET {BASE_URL}/sensorTypes
{
  "time": 1750000000000,                 // 毫秒时间戳，清单版本/更新时间
  "groups": [                            // 分组结构，给后台管理界面/分组展示用
    { "group": "常用", "icon": "⭐",
      "items": [ { "id": 1, "label": "手部检测", "value": "hand" } ] }
  ],
  "flat":  [ { "label": "手部检测", "value": "hand", "group": "常用" } ],  // 扁平清单
  "map":   { "hand": "手部检测" }          // value→中文名映射（桌面端翻译授权类型用）
}
```

- `flat`：扁平清单，每项至少含 `label` / `value`，建议带 `group`。
- `map`：`value → label` 映射。
- DB 异常时：返回 200 或 500，`flat` 为空数组，并带 `"error": "DB_ERROR"`（桌面端识别后走本地兜底，不会崩）。

---

## 需要补齐的原有 24 个类型（务必全部存在，`value` 逐字一致、不可改）

| 序号 | 分组 | value | label（中文名） |
| --- | --- | --- | --- |
| 1 | 常用 | `hand` | 手部检测 |
| 2 | 关怀 | `jqbed` | 小床监测 |
| 3 | 关怀 | `petCare` | 宠物看护 |
| 4 | 关怀 | `petCareMini` | mini看护 |
| 5 | lab | `bed4096` | OneStep |
| 6 | 定制 | `smallBedNoAlg` | 小床检测(数据) |
| 7 | 定制 | `smallBed12B` | 小床检测(12B) |
| 8 | 定制 | `tempFullBed` | 温度全床系统 |
| 9 | 定制 | `wholeChair` | 整椅展示 |
| 10 | 定制 | `minzhen` | 轮椅 |
| 11 | 精密 | `handSinglePoint` | 32*32(检测点) |
| 12 | 精密 | `hand0205` | 触觉手套 |
| 13 | 精密 | `hand0205Double` | 触觉手套2 |
| 14 | 精密 | `handGlove115200` | 触觉手套(115200) |
| 15 | 精密 | `handGloveFullPacket` | 触觉手套(整包) |
| 16 | 精密 | `smallSample` | 10*10小样 |
| 17 | 精密 | `robot1` | 宇树G1触觉上衣 |
| 18 | 精密 | `robotSY` | 松延N2触觉上衣 |
| 19 | 精密 | `robotLCF` | 零次方H1触觉上衣 |
| 20 | 精密 | `footVideo` | 触觉足底 |
| 21 | 精密 | `daliegu` | 14x20高速 |
| 22 | 精密 | `fast256` | 16x16高速 |
| 23 | 精密 | `fast1024` | 32x32高速 |
| 24 | 精密 | `humanBody` | 人体全身 |

---

## 数据库初始化 / 补齐种子（JSON，可直接 import / upsert）

> 用 `value` 作唯一键 upsert：已存在的跳过，缺失的插入。不要删除/覆盖已有记录的 `value`。

```json
[
  { "group": "常用", "icon": "⭐", "items": [
    { "label": "手部检测", "value": "hand" }
  ]},
  { "group": "关怀", "icon": "❤️", "items": [
    { "label": "小床监测", "value": "jqbed" },
    { "label": "宠物看护", "value": "petCare" },
    { "label": "mini看护", "value": "petCareMini" }
  ]},
  { "group": "lab", "icon": "🧪", "items": [
    { "label": "OneStep", "value": "bed4096" }
  ]},
  { "group": "定制", "icon": "⚙️", "items": [
    { "label": "小床检测(数据)", "value": "smallBedNoAlg" },
    { "label": "小床检测(12B)", "value": "smallBed12B" },
    { "label": "温度全床系统", "value": "tempFullBed" },
    { "label": "整椅展示", "value": "wholeChair" },
    { "label": "轮椅", "value": "minzhen" }
  ]},
  { "group": "精密", "icon": "🔬", "items": [
    { "label": "32*32(检测点)", "value": "handSinglePoint" },
    { "label": "触觉手套", "value": "hand0205" },
    { "label": "触觉手套2", "value": "hand0205Double" },
    { "label": "触觉手套(115200)", "value": "handGlove115200" },
    { "label": "触觉手套(整包)", "value": "handGloveFullPacket" },
    { "label": "10*10小样", "value": "smallSample" },
    { "label": "宇树G1触觉上衣", "value": "robot1" },
    { "label": "松延N2触觉上衣", "value": "robotSY" },
    { "label": "零次方H1触觉上衣", "value": "robotLCF" },
    { "label": "触觉足底", "value": "footVideo" },
    { "label": "14x20高速", "value": "daliegu" },
    { "label": "16x16高速", "value": "fast256" },
    { "label": "32x32高速", "value": "fast1024" },
    { "label": "人体全身", "value": "humanBody" }
  ]}
]
```

---

## 实现要求（给后台开发/AI 的硬性约束）

1. **补齐**：用上面的 JSON 按 `value` upsert，把缺失的原有类型全部补回，保证 `/sensorTypes` 至少包含这 24 个 `value`。
2. **只增不删、不改 value**：这 24 个是基线，不提供删除 / 改 `value` 的入口（改 label 可以）。管理员可以**新增**类型。
3. **`/sensorTypes` 返回**：从数据库实时聚合 `groups` / `flat` / `map` 三种形态一起返回；`time` 用最近一次类型变更时间戳。
4. **CORS**：保持开启。
5. **DB 异常兜底**：查不到 / 出错时返回 `flat: []` + `error: "DB_ERROR"`，不要抛裸 500。
6. **分组保持**：分组用 常用 / 关怀 / lab / 定制 / 精密（与桌面端授权页一致）；新增类型可放入这些组或新建组。

---

## 自测

```bash
curl {BASE_URL}/sensorTypes
# 期望：groups/flat/map 三个字段都在；flat / map 至少包含上面 24 个 value；
#       每个 value 都能在 map 里翻成对应中文名。
```
