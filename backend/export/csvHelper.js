/**
 * 兼容壳：本模块已经搬进 `@shroom/backend`，真实实现在 `sdk/backend/export/csvHelper.js`。
 *
 * 传感器帧与压力统计两种 CSV writer，以及导出文件名规则。
 *
 * 新代码请直接写包名 `require('@shroom/backend/...')`；这里保留只是为了让现有调用点不用改。
 */
module.exports = require('@shroom/backend/export/csvHelper.js');
