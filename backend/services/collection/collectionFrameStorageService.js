/**
 * 兼容壳：本模块已经搬进 `@shroom/backend`，真实实现在 `sdk/backend/collection/collectionFrameStorageService.js`。
 *
 * 一帧到底存不存的三条件判定，以及入库载荷构造。
 *
 * 新代码请直接写包名 `require('@shroom/backend/...')`；这里保留只是为了让现有调用点不用改。
 */
module.exports = require('@shroom/backend/collection/collectionFrameStorageService.js');
