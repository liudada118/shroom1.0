# Shroom 产品需求文档

## 1. 摘要

Shroom 是一个桌面端压力传感器数据采集、处理、可视化和导出系统。它通过串口连接不同形态的压力传感器，把实时压力矩阵展示为二维热力图、数字矩阵、趋势指标和三维模型，并支持本地存储、历史回放、中文表头数据导出、授权控制和自动更新。

本文档用于统一 Shroom 当前产品版本的目标、用户、范围、技术链路、系统类型处理规则和发布计划。本文档只描述当前项目已经具备或代码中已有明确分支的能力，未确认的未来能力会单独标为后续版本。

## 2. 联系人

| 姓名 | 角色 | 说明 |
| --- | --- | --- |
| 产品负责人 | 产品负责人 | 明确传感器接入优先级、用户场景和稳定版范围 |
| 前端负责人 | 前端工程师 | 负责主系统页、二维/三维展示、交互、中文文案和导出弹窗 |
| 后端负责人 | 后端工程师 | 负责桌面主进程、串口、网页通信、本地数据库、导出和授权逻辑 |
| 算法负责人 | 算法工程师 | 负责床垫、宠物看护、生命体征、矩阵线序和滤波处理 |
| 测试负责人 | 测试工程师 | 负责串口模拟、全传感器回归、界面截图和打包验收 |
| 业务/客户代表 | 现场代表 | 提供真实硬件、采集流程、导出字段和现场反馈 |

## 3. 背景

### 3.1 项目背景

Shroom 面向压力传感器硬件的数据展示和分析。项目使用桌面壳、前端页面和后端服务组合实现：后端通过串口接收传感器数据，通过网页通信推送给前端；前端使用画布、图表和三维模型展示压力分布。

当前项目已覆盖多类传感器和场景，包括触觉手套、触觉足底、床垫、小床监测、整椅、轮椅、人体全身、机器人触觉上衣、宠物看护、高速矩阵、温度床和若干项目定制座椅系统。

### 3.2 为什么现在需要产品需求文档

现有项目已经从单一展示工具扩展为多系统平台。继续迭代前，需要明确：

- 哪些能力属于稳定产品主流程。
- 哪些能力属于研发调试或项目专用入口。
- 每种系统类型在后端实际做了什么数据处理。
- 导出、回放、清零、校准、算法调用这些链路如何验收。
- 新增系统类型时应该遵守哪些接入规则。

### 3.3 当前证据

项目中已有测试报告和代码分支显示：

- 串口模拟端到端测试覆盖串口接收、协议解析、网页通信广播和界面渲染。
- 全传感器模拟测试覆盖多类传感器，核心传感器功能测试通过率达到可交付水平。
- 数据导出文档记录了历史记录导出、采集模式自动导出、自定义路径、导出状态回传和文件打开流程。
- 后端已经按系统类型处理不同帧长、波特率、矩阵线序、清零帧、姿态数据、温度数据和算法结果。

## 4. 目标

### 4.1 产品目标

把 Shroom 打造成稳定、可配置、可交付的压力传感器桌面平台，让研发、测试和客户现场人员可以快速完成传感器连接、实时观察、数据采集、历史回放和数据导出。

### 4.2 目标价值

对客户而言，Shroom 降低了传感器调试和数据观察门槛。用户不需要直接读串口或写脚本，就能看到压力变化并导出数据。

对公司而言，Shroom 可以作为多个传感器项目的统一交付工具，减少重复开发展示软件的成本，并提高现场交付质量。

### 4.3 成功指标

| 指标 | 目标 |
| --- | --- |
| 核心链路稳定性 | 串口连接、数据解析、网页通信推送、前端渲染的端到端测试通过率不低于 95% |
| 系统类型覆盖 | 稳定版支持主要传感器类型的实时展示、回放和导出 |
| 导出可靠性 | 历史记录数据导出成功率不低于 95%，失败时必须显示明确原因 |
| 回放可用性 | 用户能按历史采集标签选择记录，并完成回放、删除和导出 |
| 交付可用性 | Windows 和 macOS 包含必要前端、数据库模板、算法资源和授权文件 |
| 可维护性 | 新增传感器时，只需要补充系统配置、处理分支和展示组件，不应重写主流程 |

## 5. 用户群体

### 5.1 传感器研发和测试人员

他们需要快速验证硬件是否正常发数、数据方向是否正确、压力点是否准确、帧率是否稳定。他们关心实时性、原始数据、矩阵值、串口连接、清零、校准和导出。

### 5.2 客户现场交付人员

他们需要在客户现场快速安装软件、输入授权、连接硬件、展示效果、导出报告或表格。他们关心操作简单、错误提示清楚、导出文件容易找到、应用能自动更新。

### 5.3 算法和数据分析人员

他们需要获取结构化历史数据，用于算法训练、压力趋势分析、姿态识别、生命体征或宠物看护等后续处理。他们关心字段完整、时间戳真实、线序一致、导出格式稳定。

### 5.4 约束

- 应用需要运行在本地桌面环境，依赖串口驱动和硬件连接。
- 不同传感器协议、矩阵尺寸、波特率、线序和展示模型不同。
- 部分页面属于调试或项目专用页面，不能全部作为普通用户入口。
- 传感器数据具有实时性要求，前端渲染和后端处理不能明显阻塞。
- 授权、打包、自动更新和本地资源路径会受到 Windows 和 macOS 差异影响。

## 6. 价值主张

### 6.1 对研发和测试人员

Shroom 把串口数据直接变成可观察的二维和三维画面。用户可以快速判断传感器是否接通、点位是否正确、压力变化是否符合预期。

核心收益：

- 减少写调试脚本的时间。
- 同时看到数值矩阵、热力图和模型映射。
- 能按系统类型切换不同处理逻辑。
- 能通过清零、校准和参数调节修正现场数据。

### 6.2 对现场交付人员

Shroom 提供桌面应用、授权入口、传感器选择、实时模式、回放模式和数据下载。现场人员可以直接用图形界面完成演示和数据导出。

核心收益：

- 不需要启动多个命令行工具。
- 导出时可以选择保存目录。
- 导出结果会显示成功、失败和文件位置。
- 软件可以通过自动更新减少人工分发成本。

### 6.3 对数据分析人员

Shroom 把采集数据保存在本地数据库，并支持按历史标签导出表格。导出字段包含时间、压力面积、压力总和、最大值、矩阵数据、手套姿态、温度等场景字段。

核心收益：

- 数据可追溯。
- 导出文件可被表格工具、脚本和后续算法读取。
- 回放和导出尽量保持与前端展示方向一致。
- 多路系统可以分文件导出，便于分析座面、靠背、头枕或左右手数据。

## 7. 方案

### 7.1 用户流程

#### 7.1.1 授权进入

1. 用户打开 Shroom。
2. 系统展示密钥输入页。
3. 用户输入授权密钥。
4. 系统验证授权有效期和允许的系统类型。
5. 验证通过后进入主系统页。
6. 验证失败时提示授权不可用。

#### 7.1.2 实时采集和展示

1. 用户进入主系统页。
2. 用户选择系统类型。
3. 用户选择实时模式。
4. 用户选择串口和波特率，或使用自动连接能力。
5. 后端打开串口并解析数据帧。
6. 前端接收实时数据。
7. 页面展示压力矩阵、热力图、指标和三维模型。
8. 用户可执行清零、校准、旋转视角、切换展示方式等操作。

#### 7.1.3 历史回放

1. 用户切换到回放模式。
2. 用户选择历史采集时间或标签。
3. 后端从本地数据库读取对应数据。
4. 前端按帧播放历史压力数据。
5. 用户可暂停、继续、查看指标或删除记录。

#### 7.1.4 数据导出

1. 用户选择历史记录。
2. 用户点击下载。
3. 系统打开下载配置弹窗。
4. 用户选择保存路径和导出格式。
5. 前端校验路径可写。
6. 后端再次校验路径，并按当前系统类型生成文件。
7. 系统返回导出成功或失败。
8. 用户可以打开生成的文件或打开所在文件夹。

### 7.2 整体技术链路

#### 7.2.1 启动链路

1. 桌面主进程启动后加载后端服务。
2. 后端创建本地目录，包括数据库目录、数据导出目录、图片目录和报告目录。
3. 后端读取授权配置文件，并尝试获取标准网络时间。
4. 后端启动主网页通信服务，主要使用 `19999` 端口广播实时数据。
5. 前端加载主系统页面，读取当前语言、当前系统类型和本地参数缓存。

#### 7.2.2 系统切换链路

1. 前端系统选择框改变时，向后端发送 `{ file: 系统类型 }`。
2. 后端更新全局系统类型。
3. 后端按系统类型设置默认波特率，例如手套、足底、眼部、高速类多为 `921600`，高密床垫为 `3000000`，小床 12B 为 `1500000`，其他默认 `1000000`。
4. 后端按系统类型初始化数据库。
5. 前端按系统类型选择展示组件和参数面板。

#### 7.2.3 串口采集链路

1. 用户选择串口后，后端打开座面、靠背、头枕等串口。
2. 常规系统使用帧分隔符切分数据包。
3. 小床 12B 使用专用双字节帧尾和 `2048` 字节数据包。
4. 大床使用 `1025 + 1025` 两段数据拼成 `2048` 点矩阵。
5. 后端按数据包长度和系统类型进入处理分支。
6. 处理后的数据写入实时对象，例如 `sitData`、`backData`、`headData`、`rate`、`petCare`、`petCareMini`。
7. 后端把数据广播给前端。

#### 7.2.4 实时处理链路

1. 后端先把二进制数据转成数值数组。
2. 按系统类型做线序转换、裁剪、旋转、翻转、插值、滤波或阈值过滤。
3. 如果用户已清零，则用当前帧减去清零基准帧，负值归零。
4. 如果处于采集状态，则把当前帧写入本地数据库。
5. 如果不是本地回放模式，则把当前帧推送到前端。
6. 前端更新指标面板、二维展示和三维模型。

#### 7.2.5 存储和回放链路

1. 采集开启后，后端以采集标签作为 `date` 字段写入数据库。
2. 常规系统存储处理后的压力矩阵。
3. 手套、足底和机器人类系统额外存储清零帧和姿态数据。
4. 温度床存储压力矩阵、原始压力、温度原始值、温度换算值和温度系数。
5. 回放时后端按 `date` 查询数据，按当前系统类型重建实时推送数据。
6. 前端按帧刷新画面，并同步压力、面积、时间等指标。

#### 7.2.6 导出链路

1. 前端发送下载请求，包含历史标签、保存路径和导出选项。
2. 后端校验保存目录是否可写。
3. 后端按系统类型查询数据库。
4. 后端按系统类型重新整理导出数据，保证线序和展示方向尽量一致。
5. 导出字段使用中文表头，核心字段包括秒数、矩阵最大值、时间戳、有效点数、矩阵总和、矩阵数据。
6. 多路系统会分别导出座面、靠背、头枕或左右手文件。
7. 导出完成后，后端返回导出状态、文件列表、目录和错误原因。

### 7.3 系统类型处理明细

以下内容按代码中的系统类型整理。系统类型是前后端通信中的内部标识，不等同于给用户看的中文名称。

| 系统类型 | 输入帧/通道 | 后端处理 | 前端展示 | 存储与导出 |
| --- | --- | --- | --- | --- |
| `hand` | 常规 `1024` 点矩阵 | 走小床类线序转换，把原始矩阵调整为手部展示方向；支持清零 | 手部三维模型、数字矩阵、热力图 | 按常规矩阵存储和导出 |
| `handSinglePoint` | 常规 `1024` 点矩阵 | 调用单点手部映射函数，生成检测点矩阵 | 手部检测点、原始数字矩阵 | 导出文件名前缀使用“检测点”，可导出检测点矩阵 |
| `hand0507` | `130 + 146` 双包 | 合并两包为 `256` 点压力数据，解析末尾 `16` 字节姿态，映射左右手点位 | 手套三维模型、皮肤图、数字图 | 存储压力、姿态和清零帧；导出手指、指根、手掌分段字段 |
| `hand0205` | `130 + 146` 双包 | 合并 `256` 点压力；左手走 `handL` 映射；右手走 `handR` 和右手视频点位映射；姿态字节转数值 | 触觉手套三维模型、皮肤图、数字图、原始矩阵 | 存储压力、姿态、清零帧；导出左右手文件和手部分段 |
| `hand0205Double` | 左右手双串口，`130 + 146` 双包 | 根据包类型区分左右手；第一包缓存，第二包合并；解析姿态；左右手分别减清零帧 | 双手套模型，支持一键连接双手套 | 左右手合并导出到一个文件，字段带左手/右手前缀 |
| `handGlove115200` | `130 + 146` 双包，低波特率 | 处理逻辑与触觉手套一致，但默认波特率为 `115200` | 手套三维模型、皮肤图、数字图 | 同手套类导出 |
| `handGloveFullPacket` | `274` 字节整包 | 读取前 `256` 点压力和后 `16` 字节姿态；按左右手布局映射为 `195` 点手部面片，再映射到 `32x32` 模型矩阵 | 整包手套模式，支持数字、皮肤和原始视图 | 存储原始压力、映射点、清零帧；导出手部分段和姿态 |
| `footVideo` | 左右足双串口，`130 + 146` 双包 | 左脚走 `footVideo`，右脚走 `footVideo1`；从 `256` 点中读取足底关键点，并插值到足底矩阵 | 足底三维模型、皮肤图、数字图 | 存储压力和清零帧；导出左右脚矩阵 |
| `smallSample` | `130 + 146` 双包 | 从 `256` 点中按固定映射读取 `100` 点，形成 `10x10` 小样矩阵 | 小样数字矩阵 | 按小样矩阵导出 |
| `robot1`、`robotSY`、`robotLCF`、`robot0428`、`robot` | 多为 `130 + 146` 双包 | 合并 `256` 点压力；保留原始点位；按前端机器人组件再做部位映射；支持清零 | 机器人触觉上衣、数字矩阵、皮肤图 | 存储压力、清零帧和可能的姿态字段 |
| `foot` | 常规 `1024` 点矩阵 | 走座椅座面线序处理 | 足部/矩阵展示 | 常规导出 |
| `car` | 双通道 `1024` 点 | 座面走座面线序，靠背走靠背线序 | 汽车座椅三维模型 | 座面、靠背分文件导出 |
| `car10` | 双通道 `1024` 点 | 从 `32x32` 中裁剪 `10x10` 区域；座面和靠背各自旋转/翻转到模型方向 | 量产汽车靠背/座面模型 | 专用分支导出 |
| `yanfeng10` | 双通道 `1024` 点 | 座面走延峰座面线序，靠背走延峰靠背线序 | 延峰座椅模型 | 座面、靠背分文件导出 |
| `volvo` | 三通道 `1024` 点 | 座面、靠背、头枕分别走沃尔沃线序函数；属于三路系统 | 沃尔沃座椅模型 | 座面、靠背、头枕分文件导出 |
| `carQX` | 双/三通道 `1024` 点 | 当前后端对部分通道保留原始矩阵，前端用定制模型展示 | 清线/项目定制座椅 | 按多路座椅规则导出 |
| `wholeChair` | 三通道 `1024` 点 | 座面裁 `16x16` 并旋转；靠背裁 `16x16`、翻转并高斯；头枕裁 `10x10`、翻转、旋转、高斯并缩放 | 整椅三维模型 | 座面、靠背、头枕分文件导出，回放时复用同样方向规则 |
| `sofa` | 双通道 `1024` 点 | 使用指定行列映射函数重排矩阵 | 沙发/座椅模型 | 按座面、靠背分支导出 |
| `minzhen` | 座面 `1024` 点，靠背串口可收文本帧 | 压力矩阵走小床线序；另一串口解析陀螺仪、热敏等文本字段，并计算前后/左右角度 | 轮椅三维模型，支持普通和原始数字模式 | 压力矩阵与传感器文本信息分别入库和回放 |
| `eye` | 双通道 `130 + 146` 或 `256` 点 | 左右眼分别重排 `16x16` 点阵，左右通道使用不同翻转规则 | 眼部模型 | 按双通道矩阵导出 |
| `bigBed` | 两段 `1025` 字节 | 根据尾标 `0/1` 拼接两块 `1024` 点为 `2048` 点矩阵；按一定频率入库 | 大床模型 | 导出面积、压力、压力曲线和矩阵 |
| `bed4096` | `4096` 字节 | 直接读取 `64x64` 高密矩阵 | 高密床垫原始矩阵/网页画布 | 按高密矩阵导出 |
| `bed4096num` | `4096` 字节 | 直接读取 `64x64` 高速数字矩阵 | `64x64` 高速数字展示 | 按高密矩阵导出 |
| `smallBed` | 常规 `1024` 点 | 走小床/床垫线序，行段交换；可调用生命体征算法；支持算法返回原始矩阵调试 | 小床/床垫三维模型、数字图、热力图、生命体征面板 | 存储处理后矩阵；导出时可转置并输出算法数据 |
| `smallBed12B` | `2048` 字节，双字节小端 | 每两个字节读成一个压力值，得到 `1024` 点；再走小床线序 | 小床模型、原始数字矩阵 | 文件名前缀使用 `12B`，导出转置后的矩阵 |
| `smallBed1` | 常规 `1024` 点 | 走小床 1 号线序，行段交换，保留部分旧版处理逻辑 | 小床模型 | 按小床类导出 |
| `jqbed` | 常规 `1024` 点 | 走小床线序；定时调用 Python 算法计算在床状态、呼吸率、心率、离床/坠床/坐起等事件 | 小床监测、生命体征和告警面板 | 存储矩阵；导出时转置矩阵并保留压力指标 |
| `tempFullBed` | 常规 `1024` 点 | 先走床垫线序，再从指定行列裁出 `15x12` 压力区；低于阈值置零；读取温度点并换算温度 | 温度全床系统，显示压力和温度 | 存储压力、原始压力、温度、平均温度和温度系数；导出含温度字段 |
| `petCare` | 常规 `1024` 点 | 先走小床线序；高频调用 Python 宠物看护算法；归一化姿态、体动、信号质量、呼吸、离床和在床时长；根据呼吸模拟心率 | 宠物看护面板和压力矩阵 | 存储压力矩阵；算法结果实时推送，不作为普通矩阵字段导出 |
| `petCareMini` | 常规 `1024` 点 | 处理逻辑同宠物看护迷你版，调用独立算法入口；面板会做迷你版数据归一化 | 迷你看护面板和压力矩阵 | 同宠物看护 |
| `humanBody` | 常规 `1024` 点 | 后端直接透传原始 `32x32` 数据，不做线序变换 | 人体全身皮肤图、原始数字矩阵 | 常规矩阵导出 |
| `normal` | 常规 `1024` 点 | 通用测试矩阵，主要用于正常测试展示 | 手部/矩阵测试组件 | 常规导出 |
| `fast256` | `256` 点 | 高速小矩阵展示，前端按高速组件渲染 | `16x16` 高速矩阵 | 常规导出 |
| `fast1024` | `1024` 点 | 当前后端基本透传，不做线序变换 | `32x32` 高速矩阵 | 常规导出 |
| `fast1024sit` | `1024` 点 | 走高速座面线序函数 | 高速座面矩阵 | 常规导出 |
| `normalFast` | `1024` 点 | 使用高速测试转换函数处理列方向和量程 | `32x32` 高速测试矩阵 | 常规导出 |
| `daliegu` | `142 + 158` 双包 | 合并后得到约 `280` 点高速矩阵，解析末尾姿态字节 | `14x20` 高速展示 | 常规导出 |
| `sit` | 常规 `1024` 点 | 走小床线序后做左右翻转，再做座面压力转换 | 坐垫展示 | 常规导出 |
| `sit10` | 常规 `1024` 点 | 走席悦座椅线序 | 座椅展示 | 常规导出 |
| `sit100` | 常规 `1024` 点 | 先按列做压力换算，再映射到 `100` 点座面 | 百点座面展示 | 常规导出 |
| `matCol` | 常规 `1024` 点 | 走床褥采集线序，用于采集标签数据 | 床褥采集展示 | 采集停止时可自动导出，包含标签 |
| `sitCol` | 常规 `1024` 点 | 走手部蓝牙/坐姿采集线序，用于采集标签数据 | 坐姿采集展示 | 采集停止时可自动导出，包含标签 |
| `matColPos` | 常规 `1024` 点 | 前端按床褥姿态采集入口展示，后端主要复用采集类矩阵逻辑 | 睡姿采集展示 | 按采集类数据导出 |
| `carCol` | 常规 `1024` 点 | 走车载采集线序 | 车载采集展示 | 常规导出 |
| `newHand` | 常规 `1024` 点 | 先走小床线序并左右翻转，再走新手部映射 | 新手部模型 | 常规导出 |
| `gloves`、`gloves1`、`gloves2` | 常规 `1024` 或手套相关帧 | 分别走对应手套线序函数 | 手套模型 | 常规或手套类导出 |
| `smallM` | 常规 `1024` 点 | 走小矩阵线序 | 小矩阵模型 | 常规导出 |
| `rect` | 常规 `1024` 点 | 走矩形矩阵线序 | 矩形矩阵模型 | 常规导出 |
| `short` | 常规 `1024` 点 | 走短上衣线序 | 短上衣展示 | 常规导出 |

### 7.4 关键功能要求

#### 7.4.1 授权管理

要求：

- 支持基于密钥的授权验证。
- 授权应包含有效期和允许系统类型。
- 授权失败时不允许进入核心采集能力。
- 授权相关错误要能被测试和现场人员定位。

验收标准：

- 输入有效密钥后能进入主系统页。
- 输入无效密钥后显示错误提示。
- 授权过期后不能继续处理传感器数据。
- 如果授权限制了系统类型，前端只展示允许的系统类型。

#### 7.4.2 系统类型选择和配置

要求：

- 支持多类系统类型配置。
- 每类系统类型应包含内部标识、显示名称、默认波特率、矩阵尺寸、数据处理逻辑和展示组件。
- 支持单路、双路和三路串口场景。
- 支持不同通道传输座面、靠背、头枕、左右手或左右脚数据。

验收标准：

- 用户能从界面选择目标系统。
- 选择后后端使用正确处理逻辑。
- 串口连接失败时有可读提示。
- 多路系统能分别连接和断开。

#### 7.4.3 实时数据采集

要求：

- 后端通过串口读取硬件数据。
- 支持常规分隔符切帧。
- 支持不同数据帧长度，如 `256`、`1024`、`2048`、`4096`、`130 + 146`、`142 + 158`、`274`。
- 支持实时计算采集频率。
- 支持断开和重连。

验收标准：

- 模拟串口输入后，前端能收到连续数据。
- 数据长度和系统类型匹配。
- 断开串口后系统不崩溃。
- 重连后能恢复数据接收。

#### 7.4.4 二维可视化

要求：

- 支持数字矩阵展示。
- 支持热力图展示。
- 支持压力面积、平均压力、最大压力、压力总和、有效点数等指标。
- 支持平滑、阈值、颜色映射、矩阵方向处理。

验收标准：

- 压力值变化时，矩阵和热力图同步更新。
- 高压区域和低压区域颜色可区分。
- 指标值随数据变化更新。
- 不同矩阵尺寸展示不应溢出页面。

#### 7.4.5 三维可视化

要求：

- 支持三维模型加载。
- 支持把压力数据映射到手、足底、床垫、座椅、整椅、轮椅、人体、机器人触觉上衣等模型。
- 支持旋转、缩放、正面视角和重置。
- 支持部分系统的框选、清零、校准或姿态显示。

验收标准：

- 对应系统页面能加载正确模型。
- 实时压力数据能改变模型表面颜色或点位状态。
- 视角控制不影响数据持续刷新。
- 模型加载失败时不阻塞整体页面。

#### 7.4.6 历史数据存储和回放

要求：

- 使用本地数据库存储采集帧。
- 每帧保存时间戳、采集标签和压力矩阵。
- 支持按标签读取历史记录。
- 支持回放历史压力变化。
- 支持删除历史记录。

验收标准：

- 采集开始后能写入数据库。
- 停止采集后能在历史列表看到记录。
- 选择记录后能回放。
- 删除记录后列表同步更新。

#### 7.4.7 数据导出

要求：

- 支持从历史记录导出表格。
- 支持采集停止后自动导出指定场景数据。
- 支持用户选择保存目录。
- 支持导出进度和结果反馈。
- 支持打开导出的文件或所在文件夹。
- 不同系统可以有不同字段，但核心字段应尽量稳定。

验收标准：

- 导出的文件至少包含时间、压力指标和矩阵数据。
- 多路系统可按座面、靠背、头枕、左手、右手分文件或分字段导出。
- 导出路径不可写时显示失败原因。
- 导出成功后返回文件列表。

#### 7.4.8 自动更新和打包交付

要求：

- 支持 Windows 和 macOS 打包。
- 打包资源应包含前端构建产物、数据库模板和算法资源。
- 支持自动更新提示、下载进度和安装入口。

验收标准：

- Windows 安装包可安装并启动。
- macOS 包可按交付流程分发。
- 更新通知能显示版本信息和下载进度。

#### 7.4.9 中文界面

要求：

- 稳定版默认中文。
- 常见状态提示、按钮、系统名称和导出结果必须有中文文案。
- 英文可作为可选语言保留，但产品文档和中文交付版本应以中文为主。

验收标准：

- 中文环境下核心页面没有未翻译的英文操作文案。
- 导出成功、导出失败、删除成功等提示显示中文。

### 7.5 技术栈

| 层级 | 技术 |
| --- | --- |
| 桌面应用 | Electron、Electron Forge、electron-builder |
| 前端 | React、React Router、Ant Design、Zustand、i18next |
| 二维可视化 | 画布、图表库 |
| 三维可视化 | Three.js、三维模型资源 |
| 后端 | Node.js、Express、网页通信服务、串口库 |
| 数据库 | 本地 SQLite |
| 文件导出 | 表格写入库、前端下载组件、桌面进程通信 |
| 授权 | 加密授权文件、有效期校验 |
| 算法 | Python 运行时、床垫算法、宠物看护算法、足压报告算法 |
| 测试 | 自动化浏览器测试、串口模拟脚本、网页通信测试 |

### 7.6 假设

- 大多数用户会在本地桌面环境使用 Shroom，而不是独立浏览器访问。
- 现场机器有可用串口驱动，硬件协议与当前代码分支一致。
- 不同系统的数据方向和线序可以通过配置或处理函数统一。
- 表格导出是当前最重要的数据导出格式，更多格式属于后续增强。
- 部分页面是研发调试入口，稳定版需要明确哪些入口对普通用户开放。
- 授权机制需要和正式业务系统保持一致，避免测试脚本与主应用接口不一致。

## 8. 发布计划

### 8.1 首个稳定版本范围

目标：交付一个可用于研发、测试和现场演示的稳定版本。

包含：

- 授权进入。
- 主系统页。
- 系统类型选择。
- 实时模式和回放模式。
- 单路、双路、三路串口连接。
- 二维数字矩阵和热力图。
- 重点系统三维展示。
- 历史数据存储、回放、删除。
- 数据下载配置弹窗。
- 自定义导出路径。
- 导出状态、文件列表、打开文件和打开文件夹。
- Windows 和 macOS 打包。
- 自动更新提示。
- 串口模拟和全系统回归测试。

不包含：

- 所有调试页面的产品化整理。
- 云端数据同步。
- 浏览器独立部署。
- 用户权限分级。
- 多格式真实写入。
- 导出字段自由勾选。

### 8.2 后续增强版本一

目标：提升数据导出、配置和可维护性。

建议包含：

- 导出字段勾选。
- 中文表头兼容优化，降低表格工具打开乱码的风险。
- 多路导出合并完成状态，避免每个文件重复弹出结果。
- 系统配置集中管理。
- 对普通用户隐藏研发调试路由。
- 完善授权测试脚本和主应用接口同步。

### 8.3 后续增强版本二

目标：提升多项目复用能力。

建议包含：

- 新增系统类型向导。
- 传感器协议和矩阵映射配置化。
- 采集任务模板。
- 数据导出预设。
- 更完整的离线诊断日志。
- 重要页面的自动化截图回归。

### 8.4 待确认问题

- 稳定版默认开放哪些系统类型？
- 哪些系统必须支持完整三维模型，哪些只需要矩阵和热力图？
- 授权有效期和系统类型授权是否需要绑定？
- 导出字段是否需要为客户统一，还是允许按系统差异输出？
- Python 算法运行时是否需要随所有安装包默认打入？
- 现场用户是否需要“无需选择串口的一键连接”作为默认流程？

