# 真机 MVP 验收表

展示系统 id：`请填写`  
软件版本：`请填写`  
验收日期：`请填写`  
设备型号/序列号：`请填写`

## 身份与连接

- [ ] 每个物理传感器都显示正确的业务名称。
- [ ] 每个传感器显示完整 canonical channelId，例如 `cushion-demo:cushion`。
- [ ] COM 变化或重新插拔后，业务身份没有变化。
- [ ] 多串口交换插入顺序后，座椅/靠背/左右手没有串路。

## 协议与线序

- [ ] 每路协议识别结果为唯一 `matched`，或已人工确认完整协议。
- [ ] 空载、有载和边角单点按压都能稳定成帧。
- [ ] 单点按压落在正确行列，方向与实物一致。
- [ ] 线序只执行一次，没有二次旋转、翻转或错位。
- [ ] 协议原始点数、选点数和展示矩阵关系已记录。

## 展示与算法

- [ ] 热力图、数字矩阵和图表读取目标传感器自己的 outputChannel。
- [ ] 总压力、最大值、面积等统计来自协议解析和线序后的标准矩阵。
- [ ] 切换渲染模式不会改变业务统计值。
- [ ] 如使用算法，样例输入输出与 `04-algorithm.md` 一致。
- [ ] 如使用多传感器算法，缺帧、过期和时间偏差状态可解释。

## 数据闭环

- [ ] 实时画面连续运行至少 15 分钟，没有明显卡死或持续丢帧。
- [ ] 完成一次采集并停止，所有 `stored:true` 传感器都有记录。
- [ ] 回放同一时刻时，传感器身份和数值与采集时一致。
- [ ] CSV 中每路有业务标签、channelId、时间戳和数据数组。
- [ ] 抽查至少三帧：实时处理后数组、存储、回放和 CSV `realData` 一致。
- [ ] `stored:false` 的传感器只实时显示，不进入采集、回放和 CSV。

## 结果

- 验收结论：通过 / 有条件通过 / 不通过
- 未完成项目：
- 问题复现步骤：
- 日志、截图或导出文件路径：
