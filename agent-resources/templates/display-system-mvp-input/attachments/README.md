# 原始附件放置区

请把下列原始文件直接放在本目录，不要只在说明中引用一个 Agent 无法访问的位置：

- 厂家串口协议文档（PDF、Word、Markdown 或清晰图片）；
- 线序/通道对应表（Excel、CSV、PDF 或图片）；
- 至少一组真实串口抓包，优先提供原始二进制和十六进制文本；
- 传感器正反面、排线方向和按压测试位置照片；
- 需要自定义渲染时的效果参考图、GLB/GLTF 模型和本地纹理；
- 需要算法时的标定文件、模型、参数表和输入输出样例。

文件名建议使用英文、数字和连字符，避免同名覆盖。附件只用于生成当前展示系统，不要放密码、授权密钥或账号凭据。
