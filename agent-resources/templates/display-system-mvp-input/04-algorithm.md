# 算法需求（可选）

不需要算法时，只保留第一项并写“无”，Agent 应使用平台标准矩阵直接展示。

## 目标

- 需要解决的问题：无 / 滤波 / 标定 / 特征计算 / 姿态识别 / 多传感器融合 / 其它
- 期望输出数组是否改变：否
- 期望输出指标：例如总压力、重心、左右平衡度
- 可接受延迟：例如 50 ms
- 算法运行频率：跟随每帧 / 限制为每秒若干次

## 输入

- 输入 sensor id：`cushion`
- 使用原始协议值还是线序后的标准矩阵：线序后的标准矩阵
- 单帧还是时间窗口：单帧
- 多传感器触发 sensor：不适用
- 多传感器同步策略：latest / strict / 不适用
- 最大时间偏差：不适用

## 规则或公式

请在此写公式、伪代码、阈值和异常值处理方式。最好同时提供至少三组输入与期望输出。

| 场景 | 输入摘要 | 期望数组/指标 | 允许误差 |
| :--- | :--- | :--- | :--- |
| 空载 | 全部接近 0 | 总压力为 0 | 请填写 |
| 单点按压 | 请填写 | 请填写 | 请填写 |
| 满载/人体 | 请填写 | 请填写 | 请填写 |

## 模型和依赖

- 是否有模型文件：否
- 模型文件路径：`attachments/请填写`
- Python 第三方依赖：无
- 训练数据或标定文件：`attachments/请填写`

## 明确授权

- 允许 Agent 生成新的 Python 算法代码：否
- 允许 Agent 将算法包安装到当前展示系统：否
- 允许使用 API V2 生命周期和多传感器输入：否

如果这里写“是”，还必须把 `01-system.json` 中对应的 Agent 授权改为 `true`。算法必须通过平台算法宿主运行，不能写进渲染器 HTML。
