# Shroom 仓库任务协议

本文件适用于整个仓库。目标是在不降低风险覆盖的前提下，避免每个任务都重复进行全仓扫描、全量构建和重复取证。

## 默认工作模式

- **Fast**：回答、定位、只读评审、纯文档或低风险配置检查。先查 `ARCHITECTURE_INDEX.md`，再用 `rg` 定位相关调用链；除非发现风险信号，不做全仓扫描和全量构建。
- **Standard（默认）**：代码修改、缺陷修复和局部重构。检查工作区、只读受影响链路、实现修改，然后运行 `scripts/verify-changed.ps1 -Mode Standard`。
- **Full**：发布、真实性能结论，或触及下列高风险边界时使用：串口 framing/协议识别、`sensor.frame` 身份契约、采集存储/回放、WebSocket/命令路由、授权、Electron 启动/打包、依赖/锁文件、跨前后端公共 SDK。运行 `scripts/verify-changed.ps1 -Mode Full`。

时间目标是反馈检查点，不是草率停止条件：Fast 通常 3–5 分钟，Standard 通常 5–15 分钟；预计超出时先说明正在验证的高风险点。

## 开始任务

1. 运行 `git status --short`，保留用户已有改动；不要重置、覆盖或顺手格式化无关文件。
2. 读取 `ARCHITECTURE_INDEX.md` 的入口和验证矩阵。只有索引不足时，才读取 `ARCHITECTURE.md` 的相关章节。
3. 使用 `rg` / `rg --files` 定位符号、调用方和测试。默认排除 `node_modules/`、`build/`、`dist/`、`pack-resources/` 和大型模型资源。
4. 先定义受影响边界和验收条件，再修改代码。不要把“了解整个仓库”当成局部任务的前置条件。

## 注释规范

判断标准只有一条：读的人想知道**它是干什么的、做什么、怎么用**。不是写给检查清单。

**模板**（顺序固定）：一行说干什么 → 用法/要点（**只写不显而易见的，每条一行**）→ `@param` / `@returns`。

**长度按「散文行」算** —— 剥掉 `/**`、`*`、`//` 装饰后，不计空行、不计以 `@` 开头的标签行。

- ≤6 行是目标；7–10 行可接受，不要为了压而压。
- 11 行以上只给「有好几条彼此独立、改了会坏但不报错」的块；写之前先试着拆成两条一行的要点。
- `@param` 表不计入长度。一个 8 个入参的函数天然就有 10 行标签，那不叫注释太长。**6–10 行、主体是 `@param` 表的 JSDoc 一个字都不要动。**

**写什么**

- **改了会坏但不报错的地方** —— 这是本仓注释的主要价值。`backend/` 里串口、采集、回放、实时发布、授权、归零彼此的约束大多没有测试兜住。格式是「⚠️ 一句话 + 后果」，指向**现象**（「点了停止采集但一直在采」）而不是复述原理。
- 名不副实的名字（`isCar` 的真实语义是「是不是多通道」这类），以及它是硬编码清单还是读配置。
- 调用方必须知道的语义：返回值有多种形状、返回 `null` 表示「这一帧不发」而不是「出错」、魔数的来由。

**不写什么**

- 复述代码本身。
- 多段「为什么这样 / 否则会怎样 / 历史上怎么来的」推演。**不是删掉信息，是换存放位置**：机制写进 `ARCHITECTURE.md` 的对应小节与台账，代码里只留那句 ⚠️。
- **行号引用**（「见 1669 行」）。行号会漂移，指错比不指更坏 —— 引函数名或文件名。

**新增函数一律带注释**，哪怕只有一行说干什么。

**已知量测陷阱**：按「行首是不是 `@`」判标签时，多行 `@param` / `@throws` 的**续行**不以 `@` 开头，会被误算成散文。**量出 11–13 行的块必须手工数一遍再动** —— 已经因此误判过一次（`displaySystemWorkspaceService.js` 的 `save` 量出 12 行、实为 9 行，本该不动）。

**不要回头再压的块**：`backend/kernel/` 现存 16 块散文 11–16 行是**刻意留的**，每块都有多条彼此独立的 ⚠️，其中 `server.js` 的 `activateSubmittedLicenseKey` 属「用户权限与身份认证」高风险类别。清单见 `ARCHITECTURE.md` 的「`backend/` 注释回改成短式」一节。

**纯注释改动必须证明它是纯注释改动**：逐文件 `node --check`，再用词法分析器（acorn tokenizer）把 HEAD 与工作区各自压成 code-token 串比对，**一次只比一个路径**；然后跑受影响测试域。别用手写的字符状态机去剥注释 —— 正则字面量与除号的歧义必须靠语法上下文分辨，否则会报假阳性。

## 验证规则

- 通过变更路径选择测试，不按仓库大小选择测试。`verify-changed.ps1` 是统一入口。
- 后端测试优先使用项目 Electron 自带的 Node ABI；不要先用主机 Node 触发 `better-sqlite3` ABI 失败再重跑。
- Standard 运行受影响测试域；Full 才运行客户端、前后端 SDK、后端、lint、临时生产构建和性能基线全套。
- 构建输出必须写入系统临时目录，不能覆盖仓库现有 `build/`。
- 只读分析不得为了“更完整”默认运行发布打包、启动常驻服务或生成大型产物。
- 代码修改后按项目规则增量更新 `ARCHITECTURE.md`；只追加受影响说明、维护记录和项目进度，不重写历史。

## 并行工作

- 普通任务最多分配两个互不重叠的子任务，例如“代码定位”和“验证”。只有明确的第三个独立边界才增加代理。
- 子任务必须限定目录、问题和停止条件；禁止多个代理同时进行同一种全仓扫描。
- 主任务持续处理可独立推进的工作，不等待代理后才开始。
- 代理首次结果应优先返回决定性证据，不为边缘问题无限扩展范围。

## 结果交付

- 先给结果和行为变化，再列验证证据。
- 明确区分：已实测、静态推断、尚需真机验证。
- Fast/Standard 未触发 Full 时，说明没有运行哪些高成本检查及原因；不要把未运行说成已通过。
- 不主动提交、推送、发布或改动用户未授权的外部状态。

